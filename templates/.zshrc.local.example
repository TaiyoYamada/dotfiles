# Copy this file to ~/.zshrc.local for machine-specific settings.
# This file is intentionally not linked or tracked with real values.

# Initialize Conda only on machines that use it.
# if [[ -x /opt/anaconda3/bin/conda ]]; then
#   eval "$(/opt/anaconda3/bin/conda shell.zsh hook)"
# fi

# export EXAMPLE_API_KEY="..."
